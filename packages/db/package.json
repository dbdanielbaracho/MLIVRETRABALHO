{
  "name": "@mlivretrabalho/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('SQL package: no build step')\"",
    "typecheck": "node -e \"console.log('SQL package: no typecheck step')\"",
    "test": "bash tests/rls-isolation.sh && bash tests/professional-interests.sh && psql \"${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/mlivretrabalho}\" -v ON_ERROR_STOP=1 -q -f migrations/0003_identity.sql && psql \"${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/mlivretrabalho}\" -v ON_ERROR_STOP=1 -q -f migrations/0004_identity_memberships.sql && psql \"${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/mlivretrabalho}\" -v ON_ERROR_STOP=1 -q -f migrations/0005_persistent_jobs.sql && psql \"${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/mlivretrabalho}\" -v ON_ERROR_STOP=1 -q -f migrations/0006_work_assignments.sql && psql \"${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/mlivretrabalho}\" -v ON_ERROR_STOP=1 -q -f migrations/0007_earnings_ledger.sql"
  }
}
