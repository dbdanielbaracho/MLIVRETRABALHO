{
  "name": "mlivretrabalho",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:test:rls": "bash packages/db/tests/rls-isolation.sh"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.9.2"
  }
}
