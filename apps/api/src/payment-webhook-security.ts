export function isFreshWebhookTimestamp(timestamp:string|undefined,nowMs=Date.now(),maxAgeSeconds=300){const ts=Number(timestamp);return Number.isFinite(ts)&&Math.abs(nowMs/1000-ts)<=maxAgeSeconds;}
export function samePaymentEvent(existing:{assignmentId:string;eventType:string;amountCents:number;providerReference?:string|null},incoming:{assignmentId?:string;eventType?:string;amountCents?:number;providerReference?:string}){return existing.assignmentId===incoming.assignmentId&&existing.eventType===incoming.eventType&&existing.amountCents===incoming.amountCents&&(existing.providerReference??null)===(incoming.providerReference??null);}
