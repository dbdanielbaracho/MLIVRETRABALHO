export function isFreshWebhookTimestamp(timestamp:string|undefined,nowMs=Date.now(),maxAgeSeconds=300){const ts=Number(timestamp);return Number.isFinite(ts)&&Math.abs(nowMs/1000-ts)<=maxAgeSeconds;}
export function samePaymentEvent(existing:{provider:string;assignmentId:string;eventType:string;amountCents:number;providerReference?:string|null;recipientProfessionalId?:string|null},incoming:{provider?:string;assignmentId?:string;eventType?:string;amountCents?:number;providerReference?:string;recipientProfessionalId?:string}){return existing.provider===incoming.provider&&existing.assignmentId===incoming.assignmentId&&existing.eventType===incoming.eventType&&existing.amountCents===incoming.amountCents&&(existing.providerReference??null)===(incoming.providerReference??null)&&(existing.recipientProfessionalId??null)===(incoming.recipientProfessionalId??null);}
