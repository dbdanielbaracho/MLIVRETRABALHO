import { useEffect, useState } from 'react';
import { Pressable, SafeAreaView, StyleSheet, Text, TextInput, View } from 'react-native';
import { authHeaders } from '../lib/session';
const API='http://localhost:3000/v1';
export default function Perfil(){const [displayName,setDisplayName]=useState(''),[homeCity,setHomeCity]=useState(''),[message,setMessage]=useState('');useEffect(()=>{void load();},[]);async function load(){const headers=await authHeaders();const r=await fetch(`${API}/professional-profile`,{headers});if(r.ok){const p=await r.json();if(p){setDisplayName(p.displayName??'');setHomeCity(p.homeCity??'');}}}async function save(){const headers=await authHeaders();const r=await fetch(`${API}/professional-profile`,{method:'PUT',headers:{...headers,'content-type':'application/json'},body:JSON.stringify({displayName,homeCity})});setMessage(r.ok?'Perfil salvo':'Não foi possível salvar');}return <SafeAreaView style={s.screen}><View style={s.content}><Text style={s.title}>Seu perfil</Text><TextInput value={displayName} onChangeText={setDisplayName} placeholder="Seu nome" style={s.input}/><TextInput value={homeCity} onChangeText={setHomeCity} placeholder="Cidade" style={s.input}/><Pressable onPress={save} style={s.button}><Text style={s.buttonText}>Salvar perfil</Text></Pressable><Text>{message}</Text></View></SafeAreaView>}
const s=StyleSheet.create({screen:{flex:1,backgroundColor:'#fff'},content:{padding:24,gap:14},title:{fontSize:30,fontWeight:'800'},input:{borderWidth:1,borderRadius:12,padding:14,fontSize:17},button:{padding:16},buttonText:{fontSize:18,fontWeight:'800'}});
