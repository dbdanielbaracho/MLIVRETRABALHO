import { useState } from 'react';
import { Pressable, SafeAreaView, StyleSheet, Text, TextInput, View } from 'react-native';
import { authenticatedTenantHeaders } from '../lib/session';
import { apiUrl } from '../lib/api';
export default function Empresa(){const [title,setTitle]=useState(''),[location,setLocation]=useState(''),[pay,setPay]=useState(''),[message,setMessage]=useState('');async function create(){const headers=await authenticatedTenantHeaders();const r=await fetch(apiUrl('/company/jobs'),{method:'POST',headers:{...headers,'content-type':'application/json'},body:JSON.stringify({title,location,payCents:Math.max(0,Math.round(Number(pay.replace(',','.'))*100))})});setMessage(r.ok?'Trabalho publicado.':'Não foi possível publicar.');if(r.ok){setTitle('');setLocation('');setPay('');}}return <SafeAreaView style={s.screen}><View style={s.content}><Text style={s.title}>Publicar trabalho</Text><TextInput style={s.input} placeholder="Função ou trabalho" value={title} onChangeText={setTitle}/><TextInput style={s.input} placeholder="Local" value={location} onChangeText={setLocation}/><TextInput style={s.input} placeholder="Valor em R$" keyboardType="decimal-pad" value={pay} onChangeText={setPay}/><Pressable style={s.button} onPress={create}><Text style={s.buttonText}>Publicar</Text></Pressable><Text>{message}</Text></View></SafeAreaView>}
const s=StyleSheet.create({screen:{flex:1,backgroundColor:'#fff'},content:{padding:24,gap:14},title:{fontSize:30,fontWeight:'800'},input:{borderWidth:1,borderRadius:12,padding:14,fontSize:17},button:{padding:16,borderWidth:1,borderRadius:14,alignItems:'center'},buttonText:{fontSize:18,fontWeight:'800'}});
