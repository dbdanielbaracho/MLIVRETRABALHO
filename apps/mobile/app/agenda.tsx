import { useEffect, useState } from 'react';
import { Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native';
import { authenticatedTenantHeaders } from '../lib/session';
type Assignment={id:string;status:string;title:string;location?:string;startsAt?:string;payCents?:number};
const API='http://localhost:3000/v1';
export default function Agenda(){const [items,setItems]=useState<Assignment[]>([]),[message,setMessage]=useState('');useEffect(()=>{void load();},[]);async function load(){const headers=await authenticatedTenantHeaders();const r=await fetch(`${API}/assignments/mine`,{headers});if(r.ok)setItems(await r.json());else setMessage('Não foi possível carregar sua agenda.');}async function action(a:Assignment){const endpoint=a.status==='confirmed'?'check-in':a.status==='checked_in'?'start':a.status==='in_progress'?'check-out':a.status==='checked_out'?'complete':null;if(!endpoint)return;const headers=await authenticatedTenantHeaders();const r=await fetch(`${API}/assignments/${a.id}/${endpoint}`,{method:'POST',headers});if(r.ok){setMessage(endpoint==='check-in'?'Check-in realizado':endpoint==='check-out'?'Check-out realizado':'Status atualizado');await load();}else setMessage('Não foi possível atualizar agora.');}return <SafeAreaView style={s.screen}><View style={s.content}><Text style={s.title}>Minha agenda</Text>{items.length===0?<Text style={s.empty}>Quando uma oportunidade for confirmada, ela aparecerá aqui com horário, local e ação de check-in.</Text>:items.map(a=><View key={a.id} style={s.card}><Text style={s.role}>{a.title}</Text><Text>{a.location??'Local a confirmar'}</Text><Text>{a.startsAt?new Date(a.startsAt).toLocaleString('pt-BR'):'Horário a confirmar'}</Text><Text>Status: {a.status}</Text>{a.status!=='completed'&&<Pressable onPress={()=>action(a)}><Text style={s.action}>{a.status==='confirmed'?'Fazer check-in':a.status==='checked_in'?'Iniciar trabalho':a.status==='in_progress'?'Fazer check-out':'Concluir trabalho'}</Text></Pressable>}</View>)}<Text>{message}</Text></View></SafeAreaView>}
const s=StyleSheet.create({screen:{flex:1,backgroundColor:'#fff'},content:{padding:24,gap:16},title:{fontSize:30,fontWeight:'800'},empty:{fontSize:17,lineHeight:25},card:{padding:20,borderWidth:1,borderRadius:16,gap:10},role:{fontSize:22,fontWeight:'800'},action:{fontSize:18,fontWeight:'800',marginTop:8}});
